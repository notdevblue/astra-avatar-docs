> **기록 시점 안내:** 아래는 해당 단계 당시의 보고서입니다. 후속 결정은 [최신 상태](../../STATUS.md)를 기준으로 읽어주세요. 모델·원본 텍스처·검증 원시 데이터는 이 문서 공유본에 포함하지 않습니다.

# v173 — 자동 보정 계산 단계 축소

원래24개 보정과 Contacts94개, 가상 센서 배치, 제약30개, 물리/메시를 유지했다. 중간 gate 파라미터 계산을 최종 shape 출력 트리에 넣어 FX4레이어136파라미터를3레이어100파라미터로 줄였다. 모든 상태의 Write Defaults는 ON이다.

별도 Unity 프리팹 `Assets/BiancaIntegration/BiancaFastCorrectiveReview.prefab`. 192자세(32×크기0.5/1/2×yaw0/137°, 루트 이동 포함) 실제 SDK Contacts/Animator 검사에서 최대 키 오차8.821487e-6, 즉0.00088215%였다. 원래 계산과 같은 정확도를 유지한다. 근거 `FAST_SCALE_CHECK.json`.

목표는 등급 점수를 위한 기능 삭제가 아니라 연속 보정 지연 감소다. 원래 v174 시험에서는3프레임 지연이 있었다. 빠른 후보의 실제 연속 지연은 별도 검사 후 확정한다. 아직 지연 개선 완료라고 쓰지 않는다.
