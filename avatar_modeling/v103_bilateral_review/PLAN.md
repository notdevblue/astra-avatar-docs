> **기록 시점 안내:** 아래는 해당 단계 당시의 보고서입니다. 후속 결정은 [최신 상태](../../STATUS.md)를 기준으로 읽어주세요. 모델·원본 텍스처·검증 원시 데이터는 이 문서 공유본에 포함하지 않습니다.

# v103 — 양쪽 보정 통합과 사각면 경계 보호

v102의 좌우 독립 표면/간격 보정을 기존 v089의 두 Shape Key로 합친다. v100 잔여 new_95 원래면15415는 단순 오차가 아니었다. baseline은 대각선 [28810,28806]으로 면적비1.430/0.700, 보정 후 다른 대각선으로2.174/0.108이었다. 이 사각면 중 실제 수정 영역에 들어간 정점은28805 하나였다. 이 정점과 대응 오른쪽16737은 포즈 보정을0으로 고정해 원래 사각면 변형을 보존한다.

회전 quaternion 속성의 길이가 중간 애니메이션에서1이 아닐 수 있으므로, 드라이버의 방향 비교는 길이로 정규화한다. 원래 중립 기하·UV·웨이트·본·재질은 그대로다. 새 메시/정점/면/본0, 기존 정점의 포즈 키2개. 정적 보정 합성, 원래면 경계, 실제 회전·중간 프레임, 원본과 새 표본을 함께 검증한다. 런타임 제약/보정 드라이버 이전은 별도이며 사용자 채택을 뜻하지 않는다.
