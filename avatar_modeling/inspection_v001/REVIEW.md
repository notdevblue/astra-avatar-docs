> **기록 시점 안내:** 아래는 해당 단계 당시의 보고서입니다. 후속 결정은 [최신 상태](../../STATUS.md)를 기준으로 읽어주세요. 모델·원본 텍스처·검증 원시 데이터는 이 문서 공유본에 포함하지 않습니다.

# 새 Bianca 모델 — 가져오기·외형·변형 검수

2026-09-09. **검수 완료 / 수정 전 시제품.** 생성 원본을 보존했고, 원본 메시를 재생성하거나 정점·면·UV를 수정하지 않았다. 기존 `old_` 모델은 제작 기준으로 사용하지 않았다.

## 판단

전신 시제품으로 활용할 수 있으나, 얼굴·헤어 텍스처와 몸 변형을 보정하기 전 최종 아바타로 채택하기 어렵다. 얼굴에는 눈·입을 둘러싼 면 흐름이 있으므로 전체 자동 리메시부터 할 근거는 없다. 먼저 분리된 정점/파트를 구분하고 웨이트를 다듬은 뒤, 남는 문제에 한해 국소 면 배치를 수정하는 순서가 적절하다.

## 확보한 원본과 수치

- 사용자 저장 ZIP과 `../input/bianca_restart_tripo_v001.zip`의 SHA-256 일치: `2b88d4af556d6752fb932248a777e09586d51a412acfad1d25e6baae882b368c`.
- Tripo FBX / Blender 옵션 / 현재 4K 텍스처. 원본 FBX와 JPG는 `../input/tripo_v001/`에 보존했다.
- 메시 1개, 재질 1개, Base Color 4096×4096 JPG 1장, 뼈대·셰이프 키 없음. 별도 노멀·러프니스 텍스처는 이 내보내기에 포함되지 않았다.
- **17,555 faces = 사각형 13,710 + 삼각형 3,845. 실제 삼각형 환산 31,265.** 전부 사각형이라고 가정했던 35,110은 실제 값이 아니다.
- FBX의 정점 34,529. 같은 위치 정점을 진단용 메모리 복사본에서 합치면 16,274로 Tripo 표시와 일치한다. 저장된 메시에는 병합을 적용하지 않았다.
- 크기 약 0.4224 × 0.2017 × 0.9995m. 새 모델의 크기는 변경하지 않았다. 이전 작업의 170cm 설정이 이 파일에 자동 적용된 것은 아니다.

## 얼굴·헤어

| 관찰 | 근거 | 다음 수정 |
|---|---|---|
| 얼굴에 검은 헤어 무늬가 칠해져 있고 헤어 일부에 피부색이 묻어 있음 | 조명 없는 Base Color 렌더와 머리카락 숨김 렌더에서도 유지 | 피부/헤어 마스크를 구분해 기존 UV에서 국소 색 보정 |
| 가려졌던 눈과 노출된 눈의 채색이 다르고, 눈 아래 밝은 원형 선과 눈꼬리 번짐이 있음 | `16_basecolor_face_hair_hidden.png` | 양눈 채색과 눈선 정리. 새로운 눈 디자인으로 임의 교체하지 않음 |
| 눈썹·속눈썹·눈 내부에 실제 돌출 조각이 있음 | 회색 렌더 `17_clay_face_hair_hidden.png` | 텍스처와 별개로 필요한 입체 부품인지 확인 후 국소 정리 |
| 헤어가 레퍼런스의 겹치는 가닥보다 두꺼운 말린 리본/덩어리로 보임 | `08_clay_face.png`, `09_clay_face_45.png` | 색만으로 해결되지 않는 실루엣 차이. 앞머리·귀 옆·목 주변부터 제한된 형상 보정 검토 |
| 원래 재질에서 반사가 강해 보임 | FBX Principled Metallic 0, Roughness 0.5, Specular IOR Level 1.0 | `18_roughness_preview_face.png`는 Roughness 0.85 시험. 피부색 번짐은 여전히 남으므로 셰이더만으로 해결되지 않음 |

머리카락 숨김 이미지는 진단용 마스크다. 머리카락을 실제 삭제하지 않았으며, 마스크 경계에 드러난 열린 상단을 새로운 결함으로 판정하지 않는다.

## 토폴로지

원본의 연결 조각 3,113개와 경계 31,605개는 내보내기 시 정점 분할의 영향을 크게 받는다. 이를 실제 구멍 수로 해석하면 안 된다.

위치 기준 1e-6 병합을 메모리에서만 적용한 진단에서는 연결 조각 67개, 경계 에지 1,239개, 세 면 이상 접점 62개, 방향 불일치 에지 4개, 면적 0인 면 3개가 나온다. 일치 위치의 독립 표면이 함께 합쳐질 수 있으므로 62개를 모두 원래 메시 결함으로 단정하지 않는다. 파트 개구부도 포함되어 있어 일괄 병합·구멍 메우기는 적절하지 않다.

- **얼굴:** 눈·입 둘레에 면 흐름이 존재한다. 눈 내부와 눈 주변의 작은 독립 조각은 표정 작업 전에 별도 확인이 필요하다. 눈 감기·입 벌림은 이번 시험에 포함하지 않았다.
- **어깨·팔꿈치:** 사각형 중심이나 방향이 바뀌는 면과 삼각형이 섞여 있다. 소매와 몸통이 함께 움직이는 구간은 웨이트 조정이 먼저다.
- **무릎:** 측면에 삼각형 집중과 불규칙한 면 간격이 보인다. 정지 실루엣의 꺾임을 따라 배치된 구조다. 굽힘 후 각짐을 검토하되, 본 위치와 웨이트 영향을 먼저 분리해야 한다.
- **손목·파트 경계:** 개구부와 분리 파트가 있다. 의도적인 경계인지 확인하고 양쪽 웨이트를 맞춰야 한다. 손가락 개별 굽힘은 미시험이다.

`bianca_restart_topology_review.blend`에는 `CONNECTED_*`, `REGION_*` 선택 그룹을 추가했다. `CHECK_*` 중 최초 가져오기 기준 그룹은 정점 분할을 포함하므로 이것만으로 결함을 확정하지 않는다. 와이어 렌더는 실제 폴리곤 에지를 선으로 그렸고, 내부 삼각화 대각선을 추가하지 않았다.

## 임시 리깅·변형 시험

파일: `rig_trial/bianca_restart_DIAGNOSTIC_RIG.blend`. **최종 리그가 아니다.** 20개 본, 수동 추정 관절 위치와 공간 기반 부드러운 웨이트로 기본 동작을 시험했다. 원래 분할 정점 중 동일 위치에는 동일한 웨이트를 주었고 최대 4개 본, 미할당 정점 0개를 검증했다.

| 타임라인 프레임 | 시험 | 관찰 |
|---|---|---|
| 1 / 121 | 기본 자세 | 원래 형상 유지 |
| 21 | 양팔 올리기 | 겨드랑이·소매에 늘어짐과 뾰족한 주름. 웨이트 추가 조정 필요 |
| 41 | 팔꿈치 약 90도 | 전신 뷰에서는 비교적 형태 유지. 국소 품질 최종 승인 아님 |
| 61 | 한쪽 무릎 90도 | 접힘이 각지고 안쪽 부피가 줄어 보임. LBS와 Preserve Volume 비교 이미지 포함 |
| 81 | 양쪽 고관절 70도 / 무릎 100도 | 재킷 자락과 허벅지의 겹침, 무릎 각짐. 실제 접촉·관통 수치 측정은 미실시 |
| 101 | 머리 30도 회전 | 기본 머리 회전 확인. 헤어 물리와 모든 각도 관통 검증은 아님 |

이 결과에는 **임시 본 위치·웨이트·스키닝 방식의 영향이 함께 있다.** 특정 늘어짐을 토폴로지 단독 원인으로 확정하지 않는다. 먼저 웨이트와 관절 위치를 보정하고, 그 뒤에도 남는 부위만 국소 면 수정/보정 셰이프 키 대상으로 삼는다. 손가락, 눈·입 표정, 립싱크, 헤어·넥타이 물리, Humanoid/Unity 실행은 미검증이다.

## 보존·재로드 검증

세 Blender 파일을 다시 열어 정점 좌표·에지·면 연결·UV 해시가 모두 동일함을 확인했다. 원본 4K 이미지는 파일 안에 패킹했다. 임시 리그 기본 자세의 최대 좌표 차이는 약 7.38e-8m이며, 저장된 웨이트 합 오차는 최대 3.67e-8이다. 자세별 이미지와 `rig_trial_report.json`, `reload_verification.json`에 근거를 남겼다.

## 권장 수정 순서

1. 얼굴·헤어의 잘못 투영된 색을 구분하고, 필요한 국소 형상 수정 범위를 정한다.
2. 정점 분할·작은 독립 파트·퇴화 면을 구분해 선택적으로 정리한다. 전체 자동 리메시는 하지 않는다.
3. 어깨·소매·골반 웨이트와 관절 위치를 개선하고 굽힘을 다시 비교한다. 남는 무릎 문제에 한해 면 배치/보정 키를 검토한다.
4. 형상과 UV 변경이 필요한 부분을 먼저 마친 뒤 최종 텍스처를 보정한다. 현재 UV를 유지할 수 있으면 유지한다.
5. 최종 몸 리깅, 손가락·얼굴 표정과 플랫폼 검증으로 진행한다.

이번 승인 범위의 가져오기·진단·임시 변형 시험은 완료했다. 실제 외형 수정과 토폴로지 수정은 적용하지 않았다.
