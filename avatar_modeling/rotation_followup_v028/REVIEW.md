> **기록 시점 안내:** 아래는 해당 단계 당시의 보고서입니다. 후속 결정은 [최신 상태](../../STATUS.md)를 기준으로 읽어주세요. 모델·원본 텍스처·검증 원시 데이터는 이 문서 공유본에 포함하지 않습니다.

# 중간 회전 웨이트 보정과 회귀 v028

2026-09-09. v025의 연속값 압축을 보정했지만 코트 관통이 증가해 **고관절/바지 후보 전체는 미채택**이다. 압축 수치를 줄이는 것만으로 채택하지 않았다.

기존705+이전에 별도였던192=897자세를 보정에 사용했다. 이전192는 이제 훈련 표본이다. 바지와 재킷의 기존 웨이트만 조정했고 원형/좌표/면/UV/노멀/본은 동일하다. 단일 정점 조정24회와 추가64회 후 남은 바지 압축3건은, 이웃 정점들을 함께 조정하는 방식으로1건까지 감소했다. 마지막 실패는 `independent_017_2_R`의 오른쪽 고관절 삼각형6214다. 그 위치·웨이트는 `localized_failures.json`의 초기3건에 기록되어 있다.

후속 평가는 보정897+새 seed280910의 연속값384=1281자세, 전체67표면 조각과 코트 접촉이다. 단일 조정 후보와 이웃 묶음 후보 모두 실제 Blender 평가 표면으로 검사했다.

| 항목 | v025 | 이웃 묶음 후보 |
|---|---:|---:|
| 면적 이상 자세 | 41 /1281 | 22 /1281 |
| 이상 삼각형·자세 누적 | 66 | 32 |
| 보정897자세의 이상 자세 | 13 | 1 |
| 새384자세의 이상 자세 | 28 | 21 |
| v025보다 면적 이상이 증가한 자세 | 기준 | 4 |
| v025보다 코트/바지 교차가 증가한 자세 | 기준 | 99 |

가장 큰 교차 증가 예시는 `fresh_160_2_L`의9→50쌍이다. 이전0이던 `motion_171_sit_90`에서도40쌍이 생겼다. 기존 면적 통과만 지키는 최적화가 옷과의 상대 위치를 보존하지 못했다. 마지막1개 삼각형을 없애려고 같은 방식을 반복 적용하지 않는다.

파일 `block/bianca_BLOCK_WEIGHTS_REVIEW.blend`는 실패 비교용이며 다음 작업의 기본 모델로 쓰지 않는다. 오른쪽 전완의 국소 부분만 별도 분리해 [v030](../joint_checkpoint_v030/REVIEW.md)에서 다시 검사했다. 왼쪽 전완은 새 각도에서 회귀가 남아 제외했다. 검사에 이미 노출된384개를 후속 선택의 완전히 독립적인 시험으로 표현하지 않는다.

새 메시·정점 이동·컷·텍스처 변경은 없다. 이번 결과는 고관절 주변 웨이트의 급한 변화와 의상 간격을 함께 제한해야 한다는 모델별 증거다. 후속은 접촉을 함께 평가하는 국소 보정 또는 기존 표면의 소량 구조 수정을 검토하되, 넓은 원형 변경으로 확대하지 않는다.

기록: `audit_before.json`, `audit_after.json`, `audit_block.json`, `block/optimization.json`. 1281은 중복을 포함한 표본 수이며 고유 결함 개수가 아니다.
