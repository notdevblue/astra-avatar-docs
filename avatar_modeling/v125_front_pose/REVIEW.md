> **기록 시점 안내:** 아래는 해당 단계 당시의 보고서입니다. 후속 결정은 [최신 상태](../../STATUS.md)를 기준으로 읽어주세요. 모델·원본 텍스처·검증 원시 데이터는 이 문서 공유본에 포함하지 않습니다.

# v125 — 앞들기 자동 보정 시험, 통합 보류

v124 SIM61_QUARTER의 기존 정점 보정을 좌우 키로 나누었다. v121의 기존 4개 키를 유지하고 V125_front.L/R 2개를 추가했다. 원형·UV·웨이트·본 변경은 없다. 상완 30도·쇄골 8도 반경의 정규화 quaternion 가중치와 기존 몸통 gate로 활성화한다.

실제 Blender MCP 평가에서 기본 24자세와 난수 160자세(seed 1250911), 총 184자세를 검사했다. 57자세에서 보정이 활성화되었다. 동일한 자세에서 새 키만 끄고 켜 비교한 결과:

- 새 면적 경고 6건 / 해소 15건.
- 새 코트 접촉 쌍 47건 / 해소 160건.
- **일반 자세 검증 실패. 최종 검수본으로 채택하지 않는다.**

건수는 자세별 사건 합이며 고유 면 수나 관통 깊이가 아니다. 전체 결과 (공유본 미포함: `CHECK_1250911.json`), 생성 (공유본 미포함: `00_build.py`), 검증 구현 (공유본 미포함: `validation.py`).

사용자가 앞들기의 관절 중심과 어깨 동반 이동을 다시 지적하여, 이 보정을 더 깎기 전에 [v126 본·웨이트 진단](../v126_shoulder_audit/START_HERE.md)으로 전환했다. 회전 경로를 확정하기 전에 이 경로에 맞춘 보정을 계속 쌓지 않는다. 파일은 실패 이력과 재현용으로 보존한다.
