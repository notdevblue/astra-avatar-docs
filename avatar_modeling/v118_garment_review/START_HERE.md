> **기록 시점 안내:** 아래는 해당 단계 당시의 보고서입니다. 후속 결정은 [최신 상태](../../STATUS.md)를 기준으로 읽어주세요. 모델·원본 텍스처·검증 원시 데이터는 이 문서 공유본에 포함하지 않습니다.

# v118 — 몸통 조건을 포함한 포즈 보정

팔·쇄골 조건에 더해 chest와 spine의 회전이 각각15도 이내일 때만 기존점 보정이 작동하도록 했다. 회전이 커질수록 부드럽게 감소한다. 큰 몸통 비틀림을 해결한 것이 아니라 이 보정의 적용 범위를 제한한 것이다. 별도 본/메시 생성0, 원래 좌표·UV·웨이트 유지.

이전542(seed1170914)는 활성80, 새 면적0/해소5·새 접촉0/해소0이다. **새542(seed1180915)**는 활성76, 새 면적1/해소3·새 접촉0/해소0으로, 여전히 완전 통과가 아니다. random63의 오른팔·몸통 혼합에서 원래면5235 경고가 추가됐다. 이 면은 앞선 v117에서도 발견됐으므로 활성 조건만으로 덮지 않고 후속에서 직접 보호한다.

실제 드라이버를0/7.5/14.9/15/25도에서 계산식과 비교해 PASS. 쿼터니언 크기0.6/1.3배에서도 gate가 같고 평가좌표 차이는 최대2.67e-7m였다. 단순히 보정이 꺼져서 통과한 것으로 보지 않도록 활성값도 확인했다.

동작 제작 도구가 원래 `animation_data_clear()`로 armature driver까지 지우는 것을 코드 검토에서 발견했다. 준비한 새 동작 스크립트는 action만 교체하고 driver 유지 여부를 검사한다. 이 버전에서는 02_body 이후 실행하지 않았으며, 실제 동작 검증은 후속에서 수행한다.

[남은 작업](NEXT_WORK.md). 이 폴더 모델은 미채택 사본이다.
