> **기록 시점 안내:** 아래는 해당 단계 당시의 보고서입니다. 후속 결정은 [최신 상태](../../STATUS.md)를 기준으로 읽어주세요. 모델·원본 텍스처·검증 원시 데이터는 이 문서 공유본에 포함하지 않습니다.

# v176 — 검증된 코트 연결을 Unity에 반영

`Assets/BiancaIntegration/BiancaSurfaceReview.prefab`은 v175 T-pose/손가락/빠른 자동 보정에 v172 기존 면 연결 두 곳을 반영한 별도 후보다. 기존4삼각형을4삼각형으로 연결만 변경했다. 새로운 메시를 생성하거나 캐릭터 형태를 다시 만들지 않았다.

기존 메시 사본의 모든 정점·웨이트·bindpose·노멀·tangent·색·UV8채널·전체 shape frame 좌표/노멀/tangent/가중치를 전수 비교해 동일함을 확인했다. 수정 부위의 원래 Unity 코너 노멀 차이도0이라 잘못된 날카로운 경계 병합이 없다. 자료 `PATCH.json`, `BUILD.json`.

T-pose Avatar의 **51개 Humanoid 매핑을 GetBoneTransform으로 실제 확인**했다. 유효 플래그만으로 판단하지 않았다.

헤어 연속 면적 경고는 원래부터 일직선인 세 정점의 삼각형이었다. 길이 약0.98mm, rest면적1.82e-12m². 서로 다른 미세 웨이트 때문에 흔들릴 때 매우 작은 면적이 생겨 비율이 커졌다. 이 하나의 극소 면을 큰 머리카락 파손13곳으로 보고하지 않는다. 캐릭터 전체 실루엣이나 헤어 물리 기능을 이를 위해 바꾸지 않는다.

Unity 최종 통합 자세·재질·패키지 검증은 후속 작업으로 이어진다. 큰 어깨·겨드랑이의 기존 접힘은 별도 잔여다.
